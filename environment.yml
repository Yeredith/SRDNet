name: srdnet-test
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy
  - scipy
  - scikit-image
  - tensorboardx
  - matplotlib
  - h5py
  - pip:
    - torchnet

# Important: install PyTorch separately after creating/activating this env.
# Example (CUDA 11.8):
# conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia -y
# Example (CPU-only):
# conda install pytorch torchvision torchaudio cpuonly -c pytorch -y
